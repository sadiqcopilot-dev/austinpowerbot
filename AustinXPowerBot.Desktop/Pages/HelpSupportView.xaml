<UserControl x:Class="AustinXPowerBot.Desktop.Pages.HelpSupportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="18">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <TextBlock Text="Help &amp; Support" FontSize="26" FontWeight="SemiBold" />
            <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,16,0,0">
                <StackPanel>
                    <TextBlock Text="Export diagnostics for troubleshooting." Style="{StaticResource MutedText}"/>
                    <Button Content="Export Diagnostics" Style="{StaticResource PrimaryButton}" Width="180" Margin="0,12,0,0"
                            Command="{Binding ExportDiagnosticsCommand}"/>
                    <TextBlock Text="{Binding Status}" Style="{StaticResource MutedText}" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
