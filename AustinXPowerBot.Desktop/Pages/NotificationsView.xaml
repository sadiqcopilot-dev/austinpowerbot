<UserControl x:Class="AustinXPowerBot.Desktop.Pages.NotificationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="18">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <TextBlock Text="Notifications Center" FontSize="26" FontWeight="SemiBold" />
            <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,16,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Recent notifications" FontWeight="SemiBold" />
                    <ListBox Grid.Row="1" Margin="0,10,0,0" BorderThickness="0" Background="Transparent"
                             ItemsSource="{Binding AppState.Notifications}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource PanelBg2Brush}" CornerRadius="8" Padding="10" Margin="0,0,0,8">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Message}" Style="{StaticResource MutedText}" Margin="0,4,0,0"/>
                                        <TextBlock Text="{Binding CreatedAtUtc, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss} UTC'}" Style="{StaticResource MutedText}" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
            <TextBlock Grid.Row="2" Text="{Binding AppState.UnreadNotificationCount, StringFormat=Unread: {0}}" Style="{StaticResource MutedText}" Margin="0,10,0,0"/>
        </Grid>
    </ScrollViewer>
</UserControl>
